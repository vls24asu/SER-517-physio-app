<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Verify Two‑Factor Authentication</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
  <link rel="stylesheet" href="/css/verify2fa.css">
</head>
<body>
  <div class="overlay">
    <form class="form-card animate__animated animate__fadeInDown" action="/twofa/verify" method="POST">
      <h1>Enter Two‑Factor Code</h1>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="error-msg"><%= error %></div>
      <% } %>

      <label for="token" class="sr-only">6‑digit code</label>
      <input
        type="text"
        id="token"
        name="token"
        class="form-control"
        placeholder="123456"
        pattern="[0-9]{6}"
        maxlength="6"
        inputmode="numeric"
        required
        autofocus
      >

      <button type="submit">Verify</button>

    </form>
  </div>
</body>
</html>
