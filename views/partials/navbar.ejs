<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="/">
      <i class="bi bi-heart-pulse"></i> PhysioApp
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <% if (currentUser) { %>
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/dashboard' ? 'active' : '' %>" href="/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPath.startsWith('/exercises') ? 'active' : '' %>" href="/exercises/search">Exercises</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/routines' ? 'active' : '' %>" href="/routines">Routines</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/favourites' ? 'active' : '' %>" href="/favourites">Favourites</a>
          </li>
          <% if (currentUser.role === 'physio') { %>
            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/physio') ? 'active' : '' %>" href="/physio">Physio Dashboard</a>
            </li>
          <% } %>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <i class="bi bi-person-circle"></i> <%= currentUser.fullName %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/profile">Profile</a></li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <form action="/logout" method="POST" class="d-inline">
                  <button type="submit" class="dropdown-item">Logout</button>
                </form>
              </li>
            </ul>
          </li>
        </ul>
      <% } else { %>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/login' ? 'active' : '' %>" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/register' ? 'active' : '' %>" href="/register">Register</a>
          </li>
        </ul>
      <% } %>
    </div>
  </div>
</nav>
